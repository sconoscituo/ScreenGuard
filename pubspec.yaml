name: screenguard
description: 핸드폰 사용량 추적 + 앱 사용 제한 Flutter 앱 (Android 위젯 포함)
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  usage_stats: ^1.3.5
  fl_chart: ^0.65.0
  home_widget: ^0.4.0
  shared_preferences: ^2.2.2
  provider: ^6.1.1
  permission_handler: ^11.0.1
  intl: ^0.18.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
